name: Build & Release

# Combined Windows installer pipeline. Fires on a v* tag push to this
# repo. Consumes the *latest* GitHub release of:
#   - k10-motorsports/prodrive-plugin   (SimHub plugin assets)
#   - k10-motorsports/prodrive-overlay  (Electron HUD, win-x64 unpacked tree)
#   - k10-motorsports/prodrive-server   (Chrome extension — optional;
#                                         skipped if no release exists)
# Builds the WinUI 3 host from source, then runs Inno Setup to bundle
# the pieces into one .exe and attaches it to a release on this repo.
#
# Required repository secrets:
#   CROSS_REPO_PAT  — PAT (or GitHub App token) with `contents:read` on
#                     prodrive-plugin, prodrive-overlay, and
#                     prodrive-server — all PRIVATE. The default
#                     GITHUB_TOKEN can't read release assets from sibling
#                     *private* repos.

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-installer:
    name: Build ${{ matrix.arch }} Installer
    runs-on: windows-latest

    strategy:
      # Don't abort the other arch if one fails — gives us partial release
      # output we can ship while we fix the failed one.
      fail-fast: false
      matrix:
        include:
          - arch: x64
            msbuild_platform: x64
            rid: win-x64
          - arch: arm64
            msbuild_platform: ARM64
            rid: win-arm64

    steps:
      - uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        shell: bash
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      # Stamp the tag version into the .csproj so the published assembly
      # reports the same version the installer ships. Without this step
      # the assembly stays at the dev-time placeholder ("0.1.0"), which
      # makes UpdateService think every release is newer than what's
      # installed — the app then re-downloads + re-runs the installer
      # on every launch, forever.
      - name: Stamp assembly version
        shell: pwsh
        run: |
          $csproj = "src/RaceCorProDrive/RaceCorProDrive.csproj"
          $v = "${{ steps.version.outputs.VERSION }}"
          $xml = Get-Content $csproj -Raw
          $patched = [regex]::Replace($xml, '<Version>[^<]*</Version>', "<Version>$v</Version>")
          if ($patched -eq $xml) { throw "Failed to stamp <Version> in $csproj — element not found" }
          Set-Content -Path $csproj -Value $patched -NoNewline
          Write-Host "Stamped $csproj with <Version>$v</Version>"

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "9.0.x"

      - name: Set up MSBuild (.NET Framework)
        uses: microsoft/setup-msbuild@v2

      - name: Show .NET SDK in use
        shell: pwsh
        run: |
          Write-Host "dotnet --version:"
          dotnet --version
          Write-Host "dotnet --list-sdks:"
          dotnet --list-sdks
          Write-Host "which dotnet:"
          (Get-Command dotnet).Source

      # ─── Download latest plugin release ────────────────────────────
      - name: Download latest prodrive-plugin release
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.CROSS_REPO_PAT }}
        run: |
          New-Item -ItemType Directory -Force -Path artifacts/plugin | Out-Null
          gh release download `
            --repo k10-motorsports/prodrive-plugin `
            --pattern "*.zip" `
            --dir artifacts/plugin
          # Unpack whatever zip the plugin release ships into a flat dir.
          $zip = Get-ChildItem artifacts/plugin/*.zip | Select-Object -First 1
          if (-not $zip) { throw "No plugin .zip found in latest release" }
          Expand-Archive -Path $zip.FullName -DestinationPath artifacts/plugin/unpacked -Force
          "PLUGIN_UNPACKED=$((Resolve-Path artifacts/plugin/unpacked).Path)" >> $env:GITHUB_ENV

      # ─── Download latest overlay release ───────────────────────────
      - name: Download latest prodrive-overlay release (win-x64)
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.CROSS_REPO_PAT }}
        run: |
          New-Item -ItemType Directory -Force -Path artifacts/overlay | Out-Null
          gh release download `
            --repo k10-motorsports/prodrive-overlay `
            --pattern "overlay-win-unpacked.zip" `
            --dir artifacts/overlay
          Expand-Archive -Path artifacts/overlay/overlay-win-unpacked.zip `
            -DestinationPath artifacts/overlay/unpacked -Force
          "HUD_UNPACKED=$((Resolve-Path artifacts/overlay/unpacked).Path)" >> $env:GITHUB_ENV

          # Stream Deck plugin ships as a separate artifact so the
          # installer can copy a real .sdPlugin folder into Elgato's
          # plugins dir. Without this, electron-builder's asar bundling
          # makes the .sdPlugin invisible to the post-install step and
          # the install silently no-ops.
          try {
            gh release download `
              --repo k10-motorsports/prodrive-overlay `
              --pattern "streamdeck-plugin.zip" `
              --dir artifacts/overlay
            if (Test-Path "artifacts/overlay/streamdeck-plugin.zip") {
              Expand-Archive -Path artifacts/overlay/streamdeck-plugin.zip `
                -DestinationPath artifacts/overlay/streamdeck -Force
              # The zip stages a top-level
              # `com.k10motorsports.racecor.overlay.sdPlugin/` folder.
              $sdDir = Join-Path (Resolve-Path artifacts/overlay/streamdeck) 'com.k10motorsports.racecor.overlay.sdPlugin'
              if (Test-Path (Join-Path $sdDir 'manifest.json')) {
                "STREAMDECK_PLUGIN=$sdDir" >> $env:GITHUB_ENV
              } else {
                Write-Host "::warning::streamdeck-plugin.zip layout unexpected — manifest.json not at $sdDir; skipping Stream Deck install."
              }
            }
          } catch {
            Write-Host "::warning::Stream Deck plugin not found in latest overlay release; installer will ship without it."
          }

      # ─── Download latest Chrome extension release (optional) ───────
      # prodrive-server publishes via .github/workflows/release-extension.yml,
      # which attaches three assets to every release tagged `extension-v*`:
      #   - racecor-iracing-sync.zip          (unpacked tree)
      #   - racecor-iracing-sync.crx          (signed for registry install)
      #   - racecor-iracing-sync.metadata.json (extension id + version)
      # The unpacked zip drives the host's Load-unpacked card; the .crx
      # + metadata drive the [Registry] entries that prompt Chrome /
      # Edge to install on next launch (see CHROME EXTENSION INSTALL
      # in installer/RaceCorProDrive.iss). Each is independently
      # optional — missing assets just disable the relevant payload.
      - name: Download latest prodrive-server Chrome extension
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.CROSS_REPO_PAT }}
        run: |
          New-Item -ItemType Directory -Force -Path artifacts/extension | Out-Null
          $unpackedReady = $false
          $crxReady = $false

          # Unpacked zip — for the host's Load-unpacked fallback flow.
          try {
            gh release download `
              --repo k10-motorsports/prodrive-server `
              --pattern "racecor-iracing-sync.zip" `
              --dir artifacts/extension
            if ($LASTEXITCODE -eq 0 -and (Test-Path "artifacts/extension/racecor-iracing-sync.zip")) {
              $unpackedReady = $true
            }
          } catch {
            Write-Host "Chrome extension zip not found in latest release; skipping unpacked variant."
          }
          if ($unpackedReady) {
            Expand-Archive -Path artifacts/extension/racecor-iracing-sync.zip `
              -DestinationPath artifacts/extension/unpacked -Force
            # The zip stages a top-level `racecor-iracing-sync/` folder
            # whose contents are what ISCC needs to glob.
            $extDir = Join-Path (Resolve-Path artifacts/extension/unpacked) 'racecor-iracing-sync'
            if (-not (Test-Path (Join-Path $extDir 'manifest.json'))) {
              throw "Extension zip layout unexpected — manifest.json not found at $extDir"
            }
            "EXTENSION_SRC=$extDir" >> $env:GITHUB_ENV
          }

          # Signed .crx + metadata — for the registry install path.
          # Both must be present for the registry [Files] / [Registry]
          # blocks to fire; otherwise we ship without the .crx and the
          # user falls back to manual Load-unpacked.
          try {
            gh release download `
              --repo k10-motorsports/prodrive-server `
              --pattern "racecor-iracing-sync.crx" `
              --dir artifacts/extension
            gh release download `
              --repo k10-motorsports/prodrive-server `
              --pattern "racecor-iracing-sync.metadata.json" `
              --dir artifacts/extension
            if ((Test-Path "artifacts/extension/racecor-iracing-sync.crx") -and
                (Test-Path "artifacts/extension/racecor-iracing-sync.metadata.json")) {
              $crxReady = $true
            }
          } catch {
            Write-Host "Signed .crx + metadata not found in latest release; skipping registry install path."
          }
          if ($crxReady) {
            $crxPath = (Resolve-Path "artifacts/extension/racecor-iracing-sync.crx").Path
            $meta = Get-Content "artifacts/extension/racecor-iracing-sync.metadata.json" -Raw | ConvertFrom-Json
            if (-not $meta.id -or -not $meta.version) {
              throw "Extension metadata missing id/version fields: $($meta | ConvertTo-Json)"
            }
            "EXTENSION_CRX_FILE=$crxPath" >> $env:GITHUB_ENV
            "EXTENSION_ID=$($meta.id)" >> $env:GITHUB_ENV
            "EXTENSION_VERSION=$($meta.version)" >> $env:GITHUB_ENV
          }

          if (-not $unpackedReady -and -not $crxReady) {
            Write-Host "::warning::Chrome extension release not available; installer will ship without {app}\ChromeExtension and without the registry install."
          }

      # ─── Workaround: drop System.Security.Permissions 6.0.0 next to
      # the in-process XAML compiler task so .NET 8 MSBuild can load it.
      - name: Restore + patch XAML compiler task deps
        shell: pwsh
        run: |
          dotnet restore src/RaceCorProDrive/RaceCorProDrive.csproj -p:RuntimeIdentifier=${{ matrix.rid }} -p:Platform=${{ matrix.msbuild_platform }}
          if ($LASTEXITCODE -ne 0) { throw "restore failed" }
          $compilerDll = Get-ChildItem -Recurse -Filter "Microsoft.UI.Xaml.Markup.Compiler.dll" -Path "$env:USERPROFILE\.nuget\packages\microsoft.windowsappsdk" -ErrorAction SilentlyContinue |
            Where-Object { $_.FullName -match "tools\\net6\.0" } |
            Select-Object -First 1
          if (-not $compilerDll) { throw "Compiler DLL not found in nuget cache" }
          $toolsDir = $compilerDll.Directory.FullName
          Write-Host "WindowsAppSDK tools dir: $toolsDir"
          $sysPerms = Get-ChildItem -Recurse -Filter "System.Security.Permissions.dll" -Path "$env:USERPROFILE\.nuget\packages\system.security.permissions" -ErrorAction SilentlyContinue |
            Where-Object { $_.FullName -match "6\.0\.0\\lib\\net6\.0" } |
            Select-Object -First 1
          if (-not $sysPerms) {
            Write-Host "System.Security.Permissions 6.0.0 not in cache; fetching"
            dotnet nuget push --help | Out-Null  # ensure dotnet is warm
            $tmpProj = New-TemporaryFile
            Remove-Item $tmpProj
            New-Item -ItemType Directory -Path $tmpProj | Out-Null
            Push-Location $tmpProj
            "<Project Sdk=""Microsoft.NET.Sdk""><PropertyGroup><TargetFramework>net6.0</TargetFramework></PropertyGroup><ItemGroup><PackageReference Include=""System.Security.Permissions"" Version=""6.0.0"" /></ItemGroup></Project>" | Set-Content "tmp.csproj"
            dotnet restore tmp.csproj
            Pop-Location
            $sysPerms = Get-ChildItem -Recurse -Filter "System.Security.Permissions.dll" -Path "$env:USERPROFILE\.nuget\packages\system.security.permissions" |
              Where-Object { $_.FullName -match "6\.0\.0\\lib\\net6\.0" } |
              Select-Object -First 1
          }
          if (-not $sysPerms) { throw "Could not obtain System.Security.Permissions 6.0.0" }
          Write-Host "Copying $($sysPerms.FullName) -> $toolsDir"
          Copy-Item -Force $sysPerms.FullName $toolsDir

      # ─── Publish WinUI host ────────────────────────────────────────
      # Using .NET Framework msbuild.exe (from VS Build Tools) so the
      # in-process CompileXaml task runs in its native .NET Framework
      # environment instead of failing on .NET Core MSBuild.
      - name: Publish native host
        shell: pwsh
        run: |
          $publishDir = (Join-Path (Get-Location) "src/RaceCorProDrive/publish/${{ matrix.rid }}")
          Write-Host "Requested publish dir: $publishDir"
          msbuild src/RaceCorProDrive/RaceCorProDrive.csproj `
            /t:Restore`;Publish `
            /p:Configuration=Release `
            /p:Platform=${{ matrix.msbuild_platform }} `
            /p:RuntimeIdentifier=${{ matrix.rid }} `
            /p:SelfContained=true `
            /p:PublishSingleFile=false `
            /p:PublishReadyToRun=false `
            /p:PublishDir=$publishDir `
            /p:UseXamlCompilerExecutable=false `
            /v:detailed `
            /bl:publish.binlog
          $publishExitCode = $LASTEXITCODE
          if ($publishExitCode -ne 0) {
            Write-Host "::group::XAML compiler input/output dump"
            foreach ($obj in @("src/RaceCorProDrive/obj/Release/win-x64", "obj/Release/win-x64")) {
              foreach ($f in @("input.json","output.json")) {
                $p = Join-Path $obj $f
                if (Test-Path $p) {
                  Write-Host "==== ${p} ===="
                  Get-Content -Raw $p | Out-Host
                } else {
                  Write-Host "(not found: $p)"
                }
              }
            }
            Write-Host "::endgroup::"
            throw "dotnet publish failed ($publishExitCode)"
          }
          if (-not (Test-Path $publishDir)) {
            Write-Host "::group::Publish exited 0 but expected dir missing — searching workspace"
            Get-ChildItem -Recurse -Directory src/RaceCorProDrive |
              Where-Object { $_.Name -eq "publish" -or $_.FullName -match "publish" } |
              ForEach-Object { Write-Host "candidate: $($_.FullName)" }
            Write-Host "::endgroup::"
            throw "Host publish dir not found: $publishDir"
          }
          Write-Host "Publish dir contents:"
          Get-ChildItem $publishDir | Select-Object -First 20 | Format-Table Name, Length
          "HOST_PUBLISH=$publishDir" >> $env:GITHUB_ENV

      - name: Upload publish binlog on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: publish-binlog-${{ matrix.arch }}
          path: publish.binlog
          if-no-files-found: ignore

      # ─── Inno Setup ────────────────────────────────────────────────
      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup --no-progress --yes

      - name: Build installer
        shell: pwsh
        run: |
          $iss = "installer/RaceCorProDrive.iss"
          $args = @(
            $iss,
            "/DAppVersion=${{ steps.version.outputs.VERSION }}",
            "/DAppArch=${{ matrix.arch }}",
            "/DHOST_PUBLISH=$env:HOST_PUBLISH",
            "/DHUD_UNPACKED=$env:HUD_UNPACKED",
            "/DPLUGIN_UNPACKED=$env:PLUGIN_UNPACKED"
          )
          # Chrome extension payloads are optional. The unpacked tree
          # drives the host's Load-unpacked card; the .crx + id +
          # version drive the registry-install path.
          if ($env:STREAMDECK_PLUGIN) {
            $args += "/DSTREAMDECK_PLUGIN=$env:STREAMDECK_PLUGIN"
          }
          if ($env:EXTENSION_SRC) {
            $args += "/DEXTENSION_SRC=$env:EXTENSION_SRC"
          }
          if ($env:EXTENSION_CRX_FILE -and $env:EXTENSION_ID -and $env:EXTENSION_VERSION) {
            $args += "/DEXTENSION_CRX=$env:EXTENSION_CRX_FILE"
            $args += "/DEXTENSION_ID=$env:EXTENSION_ID"
            $args += "/DEXTENSION_VERSION=$env:EXTENSION_VERSION"
          }
          & "C:/Program Files (x86)/Inno Setup 6/ISCC.exe" @args
          if ($LASTEXITCODE -ne 0) { throw "ISCC failed ($LASTEXITCODE)" }

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: installer/output/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
