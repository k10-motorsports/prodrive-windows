<Window
    x:Class="RaceCorProDrive.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RaceCorProDrive">

    <Grid Background="{ThemeResource ApplicationBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title bar.
             Three columns: (1) brand title left, (2) actions right, and
             (3) ~140px reserved for the system caption buttons (min/max/
             close) which Win overlays into whatever element we hand to
             SetTitleBar(...). Without this reserve column the user
             actions render *under* the close button. -->
        <Grid x:Name="AppTitleBar" Height="40" Background="Transparent" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="140" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="AppTitle" Text="RaceCor Pro Drive"
                       Margin="12,0,0,0" VerticalAlignment="Center"
                       Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                       FontSize="14" FontWeight="SemiBold" />
            <StackPanel Grid.Column="1" VerticalAlignment="Center"
                        Orientation="Horizontal" Spacing="8">
                <TextBlock x:Name="UserDisplay" Text="User"
                           Foreground="{ThemeResource ApplicationForegroundThemeBrush}"
                           VerticalAlignment="Center" FontSize="12" />
                <Button x:Name="SignOutButton" Content="Sign Out" Click="OnSignOut" />
            </StackPanel>
        </Grid>

        <!-- Authed shell: just a Frame holding the active page (DashboardPage,
             SessionsPage, LibraryPage, etc.). The pages own their own primary
             nav (LeftRail) — matches the Mac/iOS layout. -->
        <Frame x:Name="ContentFrame" Grid.Row="1" Visibility="Collapsed" Loaded="OnContentFrameLoaded" />

        <!-- Pre-auth: LoginPage rendered full-window so its own centering works. -->
        <Frame x:Name="AuthFrame" Grid.Row="1" Loaded="OnAuthFrameLoaded" />
    </Grid>
</Window>
